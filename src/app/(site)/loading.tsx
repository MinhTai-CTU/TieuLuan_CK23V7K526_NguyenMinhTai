import PreLoader from "@/components/Common/PreLoader";

/**
 * Loading component for Next.js App Router
 * Automatically shown during page transitions and data fetching
 */
export default function Loading() {
  return <PreLoader />;
}
